import React from "react";

export default function FileInput({ name, hint, placeholder }) {
  // from left-to-right:
  // text input box
  // file select button (pulls up OS file select)

  // should allow for a hint and placeholder input, as well as a name for this input
  return (
    <p>There is a file input box here.</p>
  )
}